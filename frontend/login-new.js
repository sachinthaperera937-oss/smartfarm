const form=document.querySelector('#loginForm');
const API_URL=window.location.hostname==='localhost'||window.location.hostname==='127.0.0.1'?'http://localhost:5000/api':'/api';
const demo={name:'Alex Morgan',email:'alex@smartfarm.demo',password:'demo1234'};
const accounts=()=>JSON.parse(localStorage.getItem('smartfarm-accounts')||'[]');
form.addEventListener('submit',async event=>{event.preventDefault();const email=document.querySelector('#email').value.trim().toLowerCase();const password=document.querySelector('#password').value;const button=form.querySelector('button');const errorMessage=document.querySelector('#loginError');button.disabled=true;errorMessage.textContent='';try{const response=await fetch(`${API_URL}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await response.json();if(!response.ok)throw new Error(data.message||'Unable to sign in');localStorage.setItem('token',data.token);localStorage.setItem('user',JSON.stringify(data.user||{}));localStorage.setItem('smartfarm-user',JSON.stringify({name:data.user.name,email:data.user.email}));window.location.replace('dashboard.html')}catch(error){const account=[demo,...accounts()].find(item=>item.email===email&&item.password===password);if(account){localStorage.setItem('smartfarm-user',JSON.stringify({name:account.name,email:account.email}));window.location.replace('index.html')}else{errorMessage.textContent=error.message||'Unable to sign in'}}finally{button.disabled=false}});
	form.addEventListener('submit',async event=>{event.preventDefault();const email=document.querySelector('#email').value.trim().toLowerCase();const password=document.querySelector('#password').value;const button=form.querySelector('button');const errorMessage=document.querySelector('#loginError');button.disabled=true;errorMessage.textContent='';try{const response=await fetch(`${API_URL}/auth/login`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const data=await response.json();if(!response.ok)throw new Error(data.message||'Unable to sign in');localStorage.setItem('token',data.token);localStorage.setItem('user',JSON.stringify(data.user||{}));localStorage.setItem('smartfarm-user',JSON.stringify({name:data.user.name,email:data.user.email}));window.location.replace('dashboard.html')}catch(error){errorMessage.textContent=error.message||'Unable to sign in'}finally{button.disabled=false}});
